let requiredAge = 12;
let requiredHeight = 1.40;

let joinedAge = 10;
let joinedHeight = 1.45;

if (joinedAge >= requiredAge && joinedHeight >= requiredHeight) {
    console.log("Puedes ingresar a todas las atracciones del parque! 🎢");
} else {
    console.log("Lo sentimos, no cumples con los requisitos para entrar a todos las atracciones 👦");
}