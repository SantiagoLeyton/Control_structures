let requiredAge = 12;
let requiredHeight = 1.40;

let joinedAge = 10;
let joinedHeight = 1.45;

if (joinedAge >= requiredAge && joinedHeight >= requiredHeight) {
    console.log("Can join to the games of the park! 🎢");
} else {
    console.log("Sorry you are so young to this game 👦");
}